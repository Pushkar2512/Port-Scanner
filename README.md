# Port-Scanner
In networking, "ports" are communication endpoints. A port scanner sends requests to specific ports on a target IP address and analyzes the responses to determine their status:  Open: The service is listening (e.g., a web server on port 80).  Closed: No service is active on that port.  Filtered: A firewall is blocking the request.
